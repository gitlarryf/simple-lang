namespace csnex
{
    public enum Mode
    {
        read    = 0,
        write   = 1,
    }

    public enum FileType
    {
        normal      = 0,
        directory   = 1,
        special     = 2,
    }
}
